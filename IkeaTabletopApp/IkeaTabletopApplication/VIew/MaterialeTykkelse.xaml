<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IkeaTabletopApplication.VIew"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:IkeaTabletopApplication.ViewModel"
    x:Class="IkeaTabletopApplication.VIew.MaterialeTykkelse"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:VælgTykkelseVM/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="Tykkelser" HorizontalAlignment="Left" Height="625" Margin="280,140,0,0" VerticalAlignment="Top" Width="420"  ItemsSource="{Binding TykkelseOC, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding TykkelseOC}" />
        <Button x:Name="button" HorizontalAlignment="Left" Margin="1440,820,0,0" VerticalAlignment="Top" Height="135" Width="260" Command="{Binding NavigateToFarveCommad}"/>

    </Grid>
</Page>
